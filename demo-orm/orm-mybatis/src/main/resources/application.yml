server:
  port: 8761

spring:
  datasource:
    url: jdbc:mysql://10.2.6.220:3306/zwdb?useUnicode=true&characterEncoding=UTF-8&useSSL=false&autoReconnect=true&failOverReadOnly=false&serverTimezone=GMT%2B8
    username: root
    password: backend2020
    driver-class-name: com.mysql.cj.jdbc.Driver
    type: com.zaxxer.hikari.HikariDataSource
#    initialization-mode: always
#    continue-on-error: true
#    schema:   #使用下面的sql语句
#    - "classpath:db/schema.sql"
#    data:
#    - "classpath:db/data.sql"
    hikari:
      minimum-idle: 5
      connection-test-query: SELECT 1 FROM DUAL
      maximum-pool-size: 20
      auto-commit: true
      idle-timeout: 30000
      pool-name: SpringBootDemoHikariCP
      max-lifetime: 60000
      connection-timeout: 30000
logging:
  level:
    com.zw: debug
    com.zw.mapper: trace
mybatis:
  configuration:
    # 开启下划线转驼峰(java代码匹配数据库表/列名词)
    map-underscore-to-camel-case: true
  mapper-locations: classpath:mappers/*.xml #使用的resources/mappers 下面的xml
  type-aliases-package: com.zw.orm.pojo #使用的pojo路径
#tk.mybatis 配置
#mapper:
#  mappers:
#  - tk.mybatis.mapper.common.Mapper
#  not-empty: true
#  style: camelhump
#  wrap-keyword: "`{0}`"
#  safe-delete: true
#  safe-update: true
#  identity: MYSQL
pagehelper: #mybatis 分页查询
  auto-dialect: true
  helper-dialect: mysql  #数据库
  reasonable: true
  params: count=countSql